{
  "manifest_version": 3,
  "name": "GitHub Graph Tab",
  "version": "0.1.0",
  "description": "Adds a Graph tab to GitHub repositories. Renders the commit graph with no token, OAuth, or permissions - it rides your existing github.com session.",
  "content_scripts": [
    {
      "matches": ["https://github.com/*"],
      "js": ["loader.js"],
      "css": ["src/style.css"],
      "run_at": "document_idle"
    }
  ],
  "web_accessible_resources": [
    {
      "resources": ["src/*.js"],
      "matches": ["https://github.com/*"]
    }
  ]
}
